# TMP TS BACKEND M

# deploy and install

```sh
cp .env.default .env
yarn
yarn gen-grpc-types
```

# run dev

```sh
yarn dev:server
```

# build

```sh
yarn build
# or
yarn build:server
```

# run prod

```sh
yarn prod:server
```
